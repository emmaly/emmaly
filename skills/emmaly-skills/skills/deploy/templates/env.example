# Cloudflared tunnel token (required if using cloudflared service)
CLOUDFLARED_TOKEN=

# App configuration (customize per project)
# DATABASE_URL=sqlite:///data/app.db
# APP_ENV=production
# APP_PORT=8080
